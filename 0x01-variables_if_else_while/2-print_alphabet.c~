#include<stdio.h>
/**
 * main - prin alphabets
 * Description: print lowercase alphabet
 * Return: 0
 */
main(void)
{
char c;
c = 'a';
while (c <= 'z')
{
putchar(c);
c++;
}
putchar('\n');
return (0);
}
